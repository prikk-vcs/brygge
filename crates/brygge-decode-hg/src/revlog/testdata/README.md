`sha-mbles-1.bin` (640 bytes): the SHAmbles chosen-prefix SHA-1 collision vector, copied unmodified from the
`sha1-checked` 0.10.0 crate (`tests/data/sha-mbles-1.bin`; licence MIT OR Apache-2.0, as that crate). Used only to
prove the Mercurial revision-hash check refuses a colliding input.
